services:
  ocr:
    image: ocr
    environment:
      - PORT=${PORT}
      - LLM_BASE_URL
      - LLM_API_KEY
      - LLM_MODEL
      - MISTRAL_OCR_MODEL
      - AUTH_PASSWORD
      - API_KEY
      - OCR_FILE_TTL_SECONDS
      - OCR_MAX_UPLOAD_MB
    ports:
      - "${PORT}:${PORT}"
